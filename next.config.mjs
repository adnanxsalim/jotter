/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: true,
    },
    images: {
        domains: ['kpbrjwesefindoilmtid.supabase.co'],
    },
};

export default nextConfig;
